apiVersion: v1
kind: Service
metadata:
  name: my-microservice-service
spec:
  type: LoadBalancer
  selector:
    app: my-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
